import { useState } from 'react'; import { ArrowLeft, Save } from 'lucide-react'; import { Link, useNavigate } from 'react-router-dom'; import { api } from '../lib/api';
export function NuevoExpediente() { const nav = useNavigate(); const [data, setData] = useState({ numeroExpediente: '', tipoProceso: '', responsableId: '', honorariosPactados: '' }); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); const update = (key: keyof typeof data, value: string) => setData({ ...data, [key]: value }); const submit = async (e: React.FormEvent) => { e.preventDefault(); setSaving(true); setError(''); try { await api.crearExpediente({ numeroExpediente: data.numeroExpediente, tipoProceso: data.tipoProceso, responsableId: Number(data.responsableId), ...(data.honorariosPactados ? { honorariosPactados: Number(data.honorariosPactados) } : {}) }); nav('/expedientes'); } catch (e) { setError(e instanceof Error ? e.message : 'No se pudo guardar el expediente.'); } finally { setSaving(false); } }; return <div className="max-w-2xl"><Link className="back-link" to="/expedientes"><ArrowLeft size={17} />Volver a expedientes</Link><div className="mt-6"><p className="eyebrow">NUEVO REGISTRO</p><h1 className="page-title">Crear expediente</h1><p className="page-subtitle">Ingresa los datos esenciales para abrir el caso.</p></div><form onSubmit={submit} className="card mt-8 grid gap-5 sm:grid-cols-2"><label className="field-label">Número de expediente<input className="field" placeholder="EXP-2026-001" value={data.numeroExpediente} onChange={e => update('numeroExpediente', e.target.value)} required /></label><label className="field-label">Tipo de proceso<input className="field" placeholder="Civil, laboral, penal…" value={data.tipoProceso} onChange={e => update('tipoProceso', e.target.value)} required /></label><label className="field-label">ID del responsable<input className="field" type="number" min="1" value={data.responsableId} onChange={e => update('responsableId', e.target.value)} required /></label><label className="field-label">Honorarios pactados <span className="font-normal text-slate-400">(opcional)</span><input className="field" type="number" min="0" step="0.01" value={data.honorariosPactados} onChange={e => update('honorariosPactados', e.target.value)} /></label>{error && <p className="sm:col-span-2 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="flex justify-end gap-3 sm:col-span-2"><Link to="/expedientes" className="btn-secondary">Cancelar</Link><button disabled={saving} className="btn-primary">{saving ? 'Guardando…' : <><Save size={17} />Guardar expediente</>}</button></div></form></div> }
